---
"@entwico/eslint-config": minor
---

upgrade `eslint-plugin-unicorn` to v69 — more recommended rules now fire (e.g. `prefer-observer-apis`, `require-array-sort-compare`, `no-computed-property-existence-check`). The opinionated naming rules stay off: `name-replacements` (renamed from `prevent-abbreviations`), `no-for-each` (renamed from `no-array-for-each`), and `consistent-boolean-name`.
